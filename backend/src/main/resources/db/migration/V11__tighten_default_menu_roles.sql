-- 기본 메뉴 권한을 요청대로 정확히 맞춘다.
--   관리자 : 전체
--   간부진 : 인사 탭만
--   동맹원 : 시뮬레이션만
--
-- V10 에서 시뮬팀에 간부진을 포함시켰는데, "간부진은 인사탭 관리 권한만" 이라는
-- 기준과 어긋나므로 여기서 뺀다. (V10 을 고치면 Flyway 체크섬이 깨지므로 새 파일로 보정)
--
-- 이건 어디까지나 «기본값»이다. 관리자는 «관리자» 화면에서 언제든 바꿀 수 있다.

update menu_item set allowed_roles = '{ADMIN,MEMBER}' where code = 'builder';
