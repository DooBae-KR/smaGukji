# 메모 — 새 구조와 배포 방법

## 왜 바꿨나

Render 무료 인스턴스는 **15분 아무도 안 들어오면 잠들고, 깨는 데 수십 초** 걸립니다.
예전에는 로그인부터 인사 화면까지 전부 거기 묶여 있어서, 오랜만에 들어가면
앱이 고장난 것처럼 보였습니다.

프론트만 Netlify로 옮기는 것으로는 해결되지 않습니다.
화면은 즉시 뜨지만 **첫 화면인 로그인부터 수십 초 멈춰** 오히려 더 나빠집니다.
그래서 «사람이 기다리는» 경로를 통째로 항상 켜진 쪽으로 옮겼습니다.

---

## 새 구조 — 세 개의 층

| 층 | 어디 | 무엇 | 잠드나 |
|---|---|---|---|
| **화면** | Netlify | React 앱, 카드 이미지 131장 | ❌ 항상 |
| **데이터** | Supabase | 로그인, 인사 데이터, 장수·전법 조회 | ❌ 항상 |
| **계산** | Render | 덱 추천 시뮬레이션, 시트 파싱 | ⭕ 잠들어도 됨 |

**Render가 자고 있어도 로그인·인사팀·덱 편성이 전부 됩니다.**
「분석」 버튼을 눌렀을 때만 "잠들어 있던 분석 서버를 깨우는 중" 안내가 뜹니다.
기다림을 납득할 수 있는 유일한 지점이 거기라서 그렇게 뒀습니다.

### 권한은 어디서 판단하나

이제 **DB가 직접 판단합니다**(RLS 정책). 브라우저 코드를 아무리 고쳐도
남의 동맹 데이터는 나오지 않습니다. 예전에는 서버 코드가 검사했기 때문에
검사를 빠뜨린 곳이 생기면 그대로 뚫렸습니다.

공개되는 anon 키만으로는 **아무것도 읽히지 않습니다.** 정책이 전부 막습니다.

---

## ⚠️ 가장 중요 — 기존 계정은 더 이상 안 됩니다

로그인이 Supabase Auth로 바뀌었고, 그쪽 사용자 목록은 **비어 있습니다.**

| 옛 계정 | 상태 |
|---|---|
| `admin1234` | ❌ 안 됨. 다시 가입해야 함 |
| `cjswhrnr` | ❌ 안 됨 |
| `cjswh2008` | ❌ 안 됨 |
| `cjswh` | ❌ 안 됨 |

**나쁜 소식이 아닙니다.** 이 비밀번호들은 전부 너무 약해서 어차피 바꿔야 했습니다
(`admin1234`는 ID와 비밀번호가 같았습니다). 새로 만들면서 제대로 된 것을 쓰면 됩니다.

옛 계정 테이블(`app_account`)은 DB에 남아 있지만 **접근 경로가 없습니다.**
로그인 API를 아예 지웠기 때문에 인터넷에서 건드릴 수 없습니다.

---

## 해야 할 일

### 1. Supabase 설정 한 가지 (필수)

**Authentication → Sign In / Providers → Email → "Confirm email" 을 끕니다.**

이유: 게임 아이디(`cjswhrnr`)는 이메일이 아니라서, 내부적으로
`cjswhrnr@smagukji.invalid` 형태로 바꿔 씁니다. 실재하지 않는 주소라
확인 메일을 받을 수 없고, 켜져 있으면 가입해도 로그인이 막힙니다.

### 2. anon 키 복사

**Project Settings → API Keys → `anon` `public`** 값을 복사합니다.

이 키는 **공개되는 것이 정상**입니다. 브라우저에 그대로 들어갑니다.
그것만으로는 아무 데이터도 읽히지 않습니다.

> `service_role` 키는 **절대** 쓰지 마세요. 모든 정책을 무시합니다.

### 3. Netlify 연결

1. https://netlify.com → **Add new site → Import an existing project**
2. 저장소 `DooBae-KR/smaGukji` 선택
   → 빌드 설정은 `netlify.toml`이 알려주므로 그대로 둡니다
3. **Site configuration → Environment variables** 에 3개 입력

   | 이름 | 값 |
   |---|---|
   | `VITE_SUPABASE_URL` | `https://yzjbaenqfnyqfoaxqegu.supabase.co` |
   | `VITE_SUPABASE_ANON_KEY` | (2번에서 복사한 값) |
   | `VITE_API_BASE_URL` | `https://smagukji.onrender.com/api` |

4. **Deploy**
5. 주소가 나옵니다 (예: `https://smagukji.netlify.app`)

### 4. Render 쪽에 Netlify 주소 알려주기

프론트가 다른 주소로 떨어져 나갔기 때문에, 허용 목록에 넣지 않으면
**브라우저가 모든 요청을 막습니다.**

`.env` 에 아래 줄을 추가하고 `bash ./render-deploy.sh` 를 다시 돌리면 됩니다.

```
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://smagukji.netlify.app
```

(`SUPABASE_URL` 은 이미 `.env` 에 넣어뒀습니다)

### 5. 가입하고 관리자 되기

Netlify 주소로 들어가 **회원가입** 합니다. 여기서 걸림돌이 하나 있습니다.

- 동맹을 **새로 만든** 사람만 자동으로 «간부진」이 됩니다
- **이미 있는 동맹**(도원, 천조국)에 합류하면 «동맹원»이 됩니다
- «관리자»는 가입으로 만들 수 없습니다 — 그래야 아무나 관리자를 자칭하지 못합니다

그래서 **첫 관리자는 DB에서 한 번 직접 지정**해야 합니다.
가입을 마친 뒤 Supabase **SQL Editor** 에서 아래를 실행하세요
(`여기에_내아이디` 를 가입한 아이디로 바꿉니다):

```sql
update app_profile set role = 'ADMIN' where login_id = '여기에_내아이디';
```

한 번만 하면 됩니다. 그 뒤로는 «관리자» 화면에서 다른 사람 역할을 바꿀 수 있습니다.
(제가 대신 실행해 드릴 수도 있습니다 — 가입한 아이디만 알려주세요)

---

## 알아두면 좋은 것

### 카드 이미지가 저장소에 들어왔습니다

131장(14MB)을 `frontend/public/cards/` 로 내보냈습니다.
DB에서 꺼내 Render가 내려주던 방식이었는데, 그러면 Render가 자는 동안
**카드가 전부 깨져** 화면이 통째로 망가진 것처럼 보입니다.

새 카드를 추가하면 DB에 넣은 뒤 아래를 실행하고 커밋하세요.

```
node tools/export-card-images.js
```

파일 이름이 내용 해시라, 바뀐 것만 새로 생기고 없어진 것은 지워집니다.

### 계정 «삭제»가 «사용 중지»로 바뀌었습니다

지우면 그 사람이 남긴 주의 기록의 작성자를 알 수 없게 됩니다.
중지하면 로그인만 막히고 기록은 남습니다.
(완전 삭제는 Supabase 관리자 키가 필요해서 브라우저에서는 못 합니다)

### 시트 데이터 정리할 것

동맹이 3개인데 셋 다 인원이 193명으로 같습니다. 같은 시트를 세 번 올린 것으로 보입니다.

| 서버 | 동맹 | 인원 |
|---|---|---|
| 12 | 도원 | 193 |
| 2008 | 천조국 | 193 |
| S2008 | 천조국 | 193 |

`2008/천조국` 과 `S2008/천조국` 은 서버 표기만 다른 같은 동맹일 가능성이 큽니다.
어느 것이 진짜인지는 제가 알 수 없어 그대로 뒀습니다. 정리할 것을 알려주시면 지우겠습니다.

---

## 요약

- Render가 자도 **로그인·인사팀·덱 편성은 그대로** 됩니다. 분석만 기다립니다.
- 권한을 DB가 직접 판단하므로 브라우저를 조작해도 남의 데이터는 안 나옵니다.
- **기존 계정은 다시 만들어야 합니다.** 어차피 비밀번호가 너무 약했습니다.
- 제가 못 하는 것은 **Supabase 설정 한 가지(Confirm email 끄기)와 anon 키 복사, Netlify 가입** 뿐입니다.
