# 메모 — PC 꺼도 되게 만들 수 있나?

## 결론

**네, 지금 구조 그대로 가능합니다.** 코드를 고칠 필요가 없습니다.
필요한 건 **호스팅 계정 하나** 뿐이고, 10~15분이면 끝납니다.

---

## 왜 가능한가

앱이 내 PC에 묶여 있는 부분이 없습니다.

| 항목 | 어디에 있나 | PC 꺼도 되나 |
|---|---|---|
| 데이터베이스 | **이미 Supabase 클라우드** (뭄바이) | ✅ 애초에 내 PC와 무관 |
| 카드 이미지 131장 | Supabase DB 안 (`asset_image` 테이블) | ✅ 파일이 아니라 DB에 있음 |
| 로그인 세션 | Supabase DB 안 (`app_session` 테이블) | ✅ 서버 재시작해도 유지 |
| 동맹원 193명 데이터 | Supabase DB 안 (`member_week` 테이블) | ✅ |
| 메뉴·권한 설정 | Supabase DB 안 (`menu_item`) | ✅ 소스코드가 아님 |
| 앱 자체 | JAR 파일 하나 (프론트 포함) | ✅ 클라우드에 올리면 끝 |

즉 **DB는 이미 클라우드에 있고, 앱만 옮기면 됩니다.**

### 한 가지 예외 (알아두시면 좋은 것)

바탕화면의 `천하결전` 이미지 폴더는 **최초 1회 DB에 넣을 때만** 썼습니다.
이미 131장이 DB에 들어가 있어서 클라우드에서는 그 폴더가 필요 없습니다.

> 나중에 **새 카드 이미지를 추가**할 때만 그 폴더가 다시 필요합니다.
> 그때는 내 PC에서 앱을 한 번 켜서 이미지를 넣거나, 업로드 기능을 추가하면 됩니다.

**주차 시트(MemberWeek) 업로드는 문제 없습니다.** 브라우저에서 파일을 고르는 방식이라
클라우드에 올라가 있어도 어느 PC/폰에서든 그대로 됩니다.

---

## 도커는 내 PC에 필요 없습니다

오늘 도커 설치가 실패했는데, **클라우드 배포에는 영향이 없습니다.**

- Render 같은 호스팅은 **자기네 서버에서** `Dockerfile` 을 읽어 직접 빌드합니다
- 내 PC의 도커는 제가 미리 검증해보려던 용도였을 뿐입니다

도커 실패 원인은 참고로만: WSL 리눅스 배포판이 없어서입니다.
설치하려면 관리자 권한으로 `wsl --install` 후 재부팅이 필요합니다. **안 하셔도 됩니다.**

---

## 어떻게 하면 되나 (Render 기준, 10~15분)

준비물: GitHub 계정(이미 있음) + Supabase DB 비밀번호

1. **https://render.com** 접속 → **GitHub 계정으로 가입** (무료)
2. **New → Blueprint** 클릭
3. 저장소 목록에서 **`DooBae-KR/smaGukji`** 선택
   → Render 가 저장소의 `render.yaml` 을 자동으로 읽습니다
4. 화면에 값 3개를 입력하라고 나옵니다. 아래를 그대로 넣으세요:

   | 이름 | 값 |
   |---|---|
   | `SUPABASE_DB_URL` | `jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require` |
   | `SUPABASE_DB_USER` | `postgres.yzjbaenqfnyqfoaxqegu` |
   | `SUPABASE_DB_PASSWORD` | (Supabase DB 비밀번호 — `.env` 파일에 있는 값) |

5. **Apply / Deploy** 클릭 → 5~10분 기다리면 빌드 완료
6. `https://smagukji.onrender.com` 같은 주소가 나옵니다. **이제 PC 꺼도 됩니다.**

### 무료 플랜 주의

- 15분간 아무도 안 들어오면 **잠듭니다**. 다음 첫 접속이 **30초~1분** 걸립니다.
- 그 다음부터는 정상 속도입니다.
- 항상 빠르게 하려면 월 $7 유료 플랜으로 올리면 됩니다.

---

## ⚠️ 공개 주소가 생기기 전에 꼭 할 것

지금 계정 비밀번호가 너무 약합니다. 내 PC에서만 돌 때는 괜찮았지만
**인터넷에 열리는 순간 위험합니다.**

| 계정 | 현재 비밀번호 | 문제 |
|---|---|---|
| `admin1234` | `admin1234` | **ID와 비밀번호가 같음 — 몇 초면 뚫림** |
| `cjswhrnr` | `1234` | 4자리 — 즉시 뚫림 |
| `member01` | (9자) | 상대적으로 나음 |

**배포 직후 바로 바꾸세요.** 로그인한 뒤 비밀번호 변경을 하면 됩니다.

배포 설정에 이미 넣어둔 안전장치:
- 새로 만드는 계정은 **8자 미만이면 거부**됩니다
- 기존 계정은 해시로 저장돼 있어 로그인은 계속 되지만, **그래서 더더욱 직접 바꾸셔야 합니다**

---

## 다른 방법도 준비돼 있습니다

| 방법 | 특징 | 설정 파일 |
|---|---|---|
| **Render** | 가장 쉬움. 클릭 몇 번 | `render.yaml` |
| Fly.io | 안 쓸 때 완전히 멈춰 비용 0 | `fly.toml` |
| 직접 서버 / NAS | 도커만 있으면 됨 | `Dockerfile` |

자세한 명령어는 `DEPLOY.md` 에 있습니다.

---

## 요약

- **가능합니다.** 코드 수정 없이 됩니다.
- DB는 이미 클라우드라 옮길 게 없고, 앱만 올리면 됩니다.
- 내 PC의 도커는 필요 없습니다.
- 제가 못 하는 건 **Render 가입과 로그인** 뿐입니다. 그 계정은 본인 것이어야 합니다.
- 가입해서 Blueprint 만 눌러주시면, 그 뒤에 문제가 생겼을 때 제가 이어서 잡겠습니다.
